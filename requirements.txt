numpy>=1.26
pandas>=2.2
nibabel>=5.2
rich>=13.7
scikit-image>=0.23
tifffile>=2024.2.12
# The 0.2.x/ABBA-compatible builder uses the pre-3.x BrainGlobe package API.
# BrainGlobe 3.x rejects this legacy local-atlas build/install path.
brainglobe-atlasapi==2.3.1
brainglobe-utils>=0.5.0
packaging>=24.0
scipy>=1.11
matplotlib>=3.8

# Native ABBA 0.11 / PyImageJ bridge (exactly pinned for reproducibility)
pyimagej==1.5.0
scyjava==1.10.2
JPype1==1.5.1
# PyImageJ 1.5 / ScyJava 1.10 import `from jgo import jgo`; jgo 3.x removed it.
jgo==1.0.6
